replicaCount: 2

image:
  repository: ghcr.io/example/platform-status-api
  tag: "1.0.0"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

containerPort: 3000

config:
  appEnv: default
  logLevel: info

secret:
  apiKey: changeme

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 256Mi

probes:
  liveness:
    path: /healthz
    initialDelaySeconds: 1
    periodSeconds: 5
  readiness:
    path: /readyz
    initialDelaySeconds: 1
    periodSeconds: 5

strategy:
  maxUnavailable: 0%
  maxSurge: 25%

autoscaling:
  enabled: false
  minReplicas: 2
  maxReplicas: 6
  targetCPUUtilizationPercentage: 70
