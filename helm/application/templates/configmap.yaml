apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "platform-status-api.fullname" . }}
  labels:
    {{- include "platform-status-api.labels" . | nindent 4 }}
data:
  APP_ENV: {{ .Values.config.appEnv | quote }}
  LOG_LEVEL: {{ .Values.config.logLevel | quote }}
